This plugin uses the claude/channel experimental MCP extension introduced in
Claude Code. Anthropic has not committed to a stable API for this surface. A
Claude Code update may break the wake behavior without a deprecation period.
We pin the Claude Code version in our fleet until we verify a new release is
compatible. Outside users should do the same.
